# 논문 한계 · 보고서 포인트 · 남은 작업

> 정리일: 2026-06-29 (2026-07-05 갱신: 분기 DART 전환·3.5차 실험·멀티모델 forward 반영 / **2026-08-07 갱신: 6차 실험(GPT·Claude 백테스트) 완료로 멀티모델 4종 세트 종결, app.py 잔여 작업 확정, C-5 cond1 오염 검증 신설**). 코드 동결(app.py 제외) 시점 기준.
> 실험 일지는 [experiments_log.md](experiments_log.md), Forward 운영 이력은 [forward_log.md](forward_log.md) 참고.

---

## A. 논문 한계 (Limitations 섹션에 명시)

### A-1. 데이터 한계
- **DART 실적 = 분기 기준 (2026-07 전환)**: cond4의 DART 실적은 신호일 기준 가장 최근 공시된 정기보고서를 쓴다. 손익(매출·영업이익·순이익)은 **단일분기**(그 분기 3개월치, YoY는 전년 동분기比), 4~5월 구간만 사업보고서라 연간. 재무상태표(부채/자본)는 분기말 스냅샷, 영업현금흐름은 분기 보고서 특성상 **누적치**(단일분기 손익과 기간이 다름). 공시기한(분기·반기 45일, 사업 90일)이 지난 보고서만 사용해 look-ahead 차단. 이전(연간 전용) 대비 값이 연 5구간으로 갱신돼 신호 밀도 향상.
- **분기 순이익 일부 N/A (에코프로비엠·한화에어로 소수 분기)**: 적자·전환 분기 일부는 DART `finstate_all`이 순이익 라인을 반환하지 않음(법인세차감전·주당손익만 제공) → 순이익 N/A. 표시용 필드라 실험 영향 경미(영업이익률·YoY는 정상).
- **은행·금융지주 (신한지주, KB금융)**: "매출액" 계정이 없어 매출/영업이익률/영업현금흐름은 N/A로 둠 (이자수익+수수료수익을 임의 합산하는 것은 자의적이라 배제). 과거연도(FY2021~2022, IFRS17 전환 이전)는 구양식 계정명이라 순이익·자본총계 일부도 N/A.
- **적자 연도 PER**: 적자기업의 PER은 정의상 무의미 → N/A 처리 (LG화학·에코프로비엠·LG엔솔 일부 연도). 버그 아님.
- **PBR/ROE 산출 방식**: 자본총계는 지배+비지배 합계 사용(지배주주지분 아님) → BPS 소폭 과대 가능. ROE는 DART 직접 미제공이라 PBR/PER 역산(DuPont identity).
- **애널리스트 리포트 표본**: 소형주는 리포트 수가 적음 (알테오젠 41건, 두산에너빌리티 47건). 대형주 대비 컨텍스트 밀도 차이.
- **목표주가 소스 의존**: 네이버금융 크롤링 의존. 전 리포트(~3,500건) 목표가/주가 비율 스캔으로 소스 입력오류 1건(HYBE 3,000,003) 탐지·제외. 미탐지 오류 가능성 잔존.
- **dividend_yield**: 일부 N/A이나 LLM 프롬프트 미사용이라 실험 무영향.

### A-2. 방법론 한계
- **베이스라인은 참고 벤치마크**: 컨센서스(리포트 발행일)·골든크로스(크로스 발생일)는 신호 *시점*이 LLM(월별 고정)과 달라 직접 비교가 아님. → **ablation(cond1~4, 동일 시점·동일 종목)이 주력 결과**, 베이스라인은 보조.
- **다중비교 보정 미적용**: 검정 pair마다 독립적 연구질문이라 Bonferroni 등 미적용 (한 줄 명시).
- **통계검정 표본 비독립성**: 조건 간 동일 종목·날짜 공유 → Mann-Whitney 독립 가정 위배. paired test(신호 flip 분석)는 future work.
- **Sharpe 단순 연환산**: 관측치 IID 가정, 시계열 자기상관·샘플링주기 mismatch 미보정 → 과대추정 경향.
- **cond4_blind 부분 식별**: 종목명은 가렸으나 시가총액·현재가로 LLM이 종목을 부분 식별 가능 → 완전 blind 아님.
- **초과수익 날짜 정렬**: 종목·지수가 동일 거래달력이라 가정(대형주 거래정지 무시 가능 수준).
- **표본 부족 셀**: n이 작은 셀(예: 일부 Sell n=2)의 Sharpe·통계는 불안정 → 표에 "표본부족(n=N)" 수기 주석.

### A-3. 모델·실험 설계 한계
- **단일 시장 국면**: 백테스팅 2023-01~2025-12(36개월) 대형주 20종목은 대체로 상승장 → 절대 신호가 Buy로 쏠릴 수 있음(초과수익 지표로 통제).
- **단일 보유기간**: 20거래일 기준(+5거래일 보조).
- **거래비용 미반영**: 한국 주식 왕복 ~0.25%(세금+수수료). Buy 평균수익률이 작으면 결론에 영향 가능 → 명시 또는 반영.
- **온도 고정**: temperature=0.0 (졸업작품 재현성). 모델 다양성은 멀티모델 비교에서 별도 검토.
- ~~**Gemma는 백테스트 미포함, forward만 적용**~~ → **해소 (4차, 2026-07-10)**: 무료 tier rate-limit을 감수하고 Gemma도 백테스트에 편입. **6차(2026-08-07)로 백테스트·forward 모두 4모델로 정렬되어 비대칭 없음.**
- **모델별 결측·표본 비대칭 (6차)**: Claude cond1은 응답 거절 197건으로 **n=523**(다른 조건 720) — temperature=0이라 재시도로 복구 불가. 조건 간 ablation 비교 시 명시 필요. Gemma cond1이 전량 Neutral이라 비교 자체가 불가했던 것과 같은 종류의 제약.
- **표본부족 셀 (6차)**: Claude cond2 Buy **n=6**(Hit 100%·Sharpe 5.60)·cond4_no_reports Buy **n=1**(Sharpe NaN), GPT cond2 Buy **n=21**. 평균·Hit·Sharpe를 성능으로 해석 불가 → 표·화면에 경고 병기 필수.
- **모델별 검정력 차이 (6차)**: Neutral 성향에 따라 Buy 표본이 flash-lite 320 ~ Claude 58로 5배 이상 차이 → 같은 효과 크기라도 유의성 판정이 갈림. GPT cond4의 비유의(p=0.121)는 성과 부재가 아니라 검정력 부족(n=115)으로 해석해야 함. **모델 비교 시 성과 지표와 신호 분포를 반드시 함께 제시.**

---

## B. 보고서에 쓰면 좋을 강점·분석거리

### B-1. 방법론 엄밀성 (어필 포인트)
- **Look-ahead bias 전 경로 차단**: 현재가=신호일 당일 종가, 리포트=신호 전일까지, DART=공시기한(3/31) 기준 회계연도 매핑, 컨센서스 대조군도 당일 종가로 통일.
- **실행순서 비의존성**: 주가 캐시 앞쪽 backfill로 캐시 삭제·재실행 시 결정적 재현 보장.
- **기간 정합**: EXPERIMENT_END(2025-12-31) 단일 상수로 LLM·베이스라인 동일 기간.
- **생성/평가 디커플링**: 신호는 LLM이 할 수 있는 **절대 방향** 예측, 스킬 평가는 **시장초과수익(알파)** — 시장 데이터 없는 LLM에 상대 예측을 강요하지 않음.

### B-2. 분석 거리 (결과 해석)
- **무기술 벤치마크 호명**: compare의 "전체" 신호 평균(전 종목 × 월별 20일 수익률)을 "시장참여 벤치마크"로 명명 → cond1 Buy > 벤치이면 단순 상승장 편승이 아닌 **종목 선별 효과** 논증. (코드 불필요)
- **Confidence calibration**: 신호별 confidence 이미 저장됨 → 구간별(50~69/70~89/90+) hit rate 표 = "LLM 확신도가 유의미한가" 챕터 (compare에 함수 1개 추가).
- **Sell 신호 현실 해석**: 개인투자자는 공매도 사실상 불가 → Sell을 "보유 회피/수익 실현" 신호로 framing.
- **Sell 억제 아티팩트 제거 서술**: 절대 전환 + Buy/Sell 대칭화로 Sell 비율 변화 비교 (2차 0~4% → 3차).
- **Sell 이상치 사례 심층**: 에코프로비엠 2023, LG엔솔 2024-08, 삼성SDI 2025-10 등.
- **통계 병행**: 비모수(Mann-Whitney + Cliff's δ) + 모수(Welch t + Cohen's d) + effect size.

### B-3. 재현성
- 결과 CSV 옆에 메타데이터(JSON) 저장 권장: 모델명 / temperature / 프롬프트 커밋 해시 / 실행일. (프롬프트를 여러 번 바꿨고 멀티모델 예정이라 혼동 방지)

---

## C. 남은 작업 (TODO)

> **보고 범위 (GPT 리뷰 확정)**: cond1~4 + cond4_no_reports 산출물로 서술. reports_only/dart_only/cond4_blind는 미완료 보조실험으로 분리. 성능 해석은 전체 평균이 아닌 **Buy 신호 성과·초과수익·신호 분포·컨센서스 대비 유의성** 중심.

### C-0. PER을 TTM(최근 4분기) 기준으로 전환 (**구현·재수집 완료 2026-07-11** — 5차 재실행만 남음)

- [x] **전 필드 audit 완료 (전 20종목, 전 필드, vs 네이버)**: 시총·매출·영업익·순익·영업이익률·부채비율·YoY 전부 오차 ~0%, PER TTM 후 정합(평균 3.5%). PBR은 C방식(지배주주지분÷총발행주식수) 전환으로 14.3%→1.8% 정합. ROE만 파생 잔차(8%), 배당은 LLM 미포함으로 무영향. 상세: [experiments_log.md](experiments_log.md) "데이터 검증 & PER TTM 전환" 섹션.
- [x] **PBR C방식 전환**: 지배주주지분(분기)÷총발행주식수(우선주 포함). 비지배 큰 지주·복합기업 편향 제거(현대차 −27%→+2%). 커밋 2b26c61/056d3c6.
- [x] **TTM PER 구현**: `get_ttm_eps`(단일분기 기본주당이익 합산 — 계획의 순이익재계산 대신, 분기 thstrm이 단일분기임을 실증) + `get_per_eps`(분기 XBRL 미비 시 연간 fallback). forward·백테스트·월별갱신 3경로 적용. 커밋 cd142c2/aa8b866/12308a5.
- [x] **financials 재수집** (720행): PER 결측 189→200(트레일링 적자 32건 왜곡 고PER 정상 제거 + fallback 21건 복구). PBR/자본은 연간 유지.
- [x] **5차 실험 완료** (flash-lite 2026-07-11 / Gemma 2026-07-13) — TTM+C방식 financials로 cond2/3/4/no_reports 재실행(cond1 재사용). 핵심 결론(cond4 Buy 절대수익 컨센서스 우위) 연간·TTM 양쪽·양 모델 유의 유지, 초과수익 유의성만 양 모델 공통 경계선 약화. 모델 비교 TTM 기준 갱신 완료(Gemma 결측 0·rate-limit 0). 상세는 [experiments_log.md](experiments_log.md) 5차 섹션.
- [x] **데이터 검증 문서 [prove.md](prove.md) 신설** — 3각도(라이브 20종목·저장 CSV·독립 재계산)로 전 항목 증권사 정합 증명.

**원래 계획 (2026-07-11 착수 전 기록, 참고용):**

- **동기**: 현행 PER은 최근 사업보고서(연간) EPS 기준 후행값. 실적 급변 국면에서 시장 통용치와 괴리 — 삼성전자 예: 우리 43.1(285,000/6,605=FY2025 연간EPS) vs 토스·증권사 23.2(EPS~12,350=TTM/선행). 가격은 일치(286,500≈285,000) 확인됨 → **순수 EPS 기준 차이, 버그 아님**(연간은 "틀린" 게 아니라 하나의 합법적 관습, 다만 1~3월엔 15~27개월 묵는 staleness가 문제). 시장 정합성 + 정보 신선도(LLM 판단 왜곡 완화) 위해 TTM으로 전환.
- **결정 = A(백테스트·forward 둘 다 TTM)**. B(분리: 백=연간/forward=토스)는 forward가 백테스트를 검증하는 의미를 깨서 기각. 백테스트는 토스로 못 맞춤(과거 스냅샷 없음)이라 DART 재구성 필수 → forward도 같은 DART-TTM 코드로 통일.
- **연간(C) 결과 보존**: 1~4차(연간) 결과를 **덮어쓰지 말 것** → 5차(TTM)와 병기해 "결론이 PER 방식에 무관하게 유지되는가" robustness 비교군으로 활용. (논문: 초기 연간 → 시장정합 위해 TTM 전환, 두 기준 하 ablation 결론 대조)
- **방식**: TTM EPS = TTM 순이익 ÷ 발행주식수. TTM 순이익 = 최근 연간 + 최근 분기 누적 − 전년 동기 분기 누적 (예: Q1 공시 시점 = FY(n−1) + Q1(n) − Q1(n−1)). **DART 분기 주당이익 라인은 불안정**(2024 3분기누적 1,440 < 반기누적 1,419 모순 확인)하므로 EPS 라인 직접 사용 금지 → **순이익으로 재계산**. (PER = 시가총액/TTM순이익 = 현재가/TTM_EPS)
- **look-ahead**: 신호일 기준 공시된 보고서만 사용 (기존 `applicable_dart_period` 로직 재활용).
- **영향 범위**: `collect_financials.py`(get_dart_annual→TTM 변형), `update.py`(forward 경로) 수정 → **백테스트 financials 재수집 + 5차 실험(5조건×2모델) 재실행**(PER/PBR/ROE가 cond2+ 컨텍스트). 재실행 부담 없음 확인(flash-lite·Gemma 비용 0, Gemma만 시간).
- **ROE/PBR**: PBR은 자본총계(분기말 스냅샷)라 무영향. ROE=PBR/PER 역산이라 PER 바뀌면 자동으로 TTM ROE가 됨(더 정확). 순이익 정의는 **당기순이익(연결 전체) vs 지배주주순이익** 중 토스와 맞는 쪽 확인 필요(삼성 소폭 차이).
- **★ 최우선 = 전 데이터 필드 audit (PER만이 아니라 우리가 뽑는 모든 값이 증권사와 일치하는지)**:
  - **price**: FDR raw — 문제 없을 것 (삼성 285,000≈토스 286,500 확인). 그래도 스팟 확인.
  - **dart_fundamentals** (매출/영업이익/순이익/영업이익률/부채비율/영업현금흐름/YoY): 단일분기 기준. **증권사가 분기·연간·TTM 중 무엇을 보여주는지 기준 정렬**부터 맞추고 값 대조.
  - **financials** (PER/PBR/ROE/시가총액): 핵심 문제 구간. PBR·ROE도 **지배주주 vs 연결전체 정의 차이**로 어긋날 수 있음.
  - **방법**: 대표 종목 — 대형주(삼성전자) + 금융(신한지주, 매출 N/A 엣지) + 적자 바이오(알테오젠) — 로 **필드 하나하나 증권사와 대조**, 불일치는 원인(정의 차이 vs 버그)까지 규명. 이게 5차 재실행보다 먼저.
- **작업 순서 (검증 우선 — 풀 재실행 전 계산식·데이터 확정)**:
  1. DART 분기 순이익(누적) 신뢰성 검증 + TTM 계산식 구현
  2. **전 필드 audit**(위) — 삼성/신한/알테오젠으로 토스·증권사 API와 대조, 지배주주 vs 연결 정의 확정. 토스 API는 **검증용**이지 live 소스 아님
  3. 전 필드 일치 확인되면 → financials 재수집 + 5차 실험 재실행 → compare/breakdown/significance

### C-1. 3차 실험 (완료 2026-06-29)
- [x] 5조건 실행(cond1~4 + cond4_no_reports) → compare + significance
- [x] 다축 분해(연도별·국면별) — breakdown.py, mean+median
- [x] Sell 이상치 사후 분석 — 하락장 Sell 견고(중앙값·hit·윈저라이즈 안정)

### C-2. 분석·서술 (코드 최소)
- [x] ~~거래비용 반영 여부~~ → **미구현 결정**. 왕복 ~0.25%는 near-flat 차감이라 조건 간 랭킹·유의성 불변, 무기술 벤치 대비 상쇄. 지수 대비 Buy 초과수익만 ~0.25%p 감소(양수 유지). → 논문 한 줄 명시.
- [ ] Buy & Hold(시장참여) 벤치마크 호명 — 서술. **B-1(탭3 표 재구성)과 같은 내용**이므로 함께 처리
- [ ] Confidence calibration 분석 (temp=0라 고정 가능성 — 실익 확인 후)
  - 6차 관찰: cond4 기준 confidence std가 flash-lite 3.6 vs **Claude 11.1(범위 45~76)**. 앵커 모델에서 값이 몰려 보류했으나 **Claude 한정으로는 구간별 hit rate 분석이 성립할 수 있음** → 실익 재검토
- [ ] **백테스트 vs forward 대조 프레이밍 — 보고서에만 서술, app.py 미반영** *(대화 라벨 B-3)* (2026-08-07 결정)
  - 백테스트(2023-2025, 전 모델 knowledge cutoff 이전)와 forward(2026, cutoff 이후)의 성과 격차를 **사전학습 오염 추정치**로 해석하는 서술. C-5(cond1 오염 검증)와 한 챕터로 묶음
  - app.py에 넣지 않는 이유는 기존 "미채택 — forward 성과 탭" 결정과 동일(표본 비독립, 주력 근거 오인 방지)
  - 선행 조건: forward 20거래일 성숙 (2026-08-10 전후, `forward_eval.py` 1회 실행)

### C-3. 확장
- [ ] **멀티모델 비교** (temperature=0.0 고정) — **예산 상한 5만원(~$35) 총합, 여러 번 재실행 전제**
  - **세트 확정 (4종, 제공사 3사 + 오픈)** — 괄호는 1회 비용(5조건×720콜):
    - 앵커·반복: **Gemini 2.5 Flash-Lite** (현재 주력, ~$0.67)
    - 오픈·금융평판: **Gemma 4** (AI Studio, 무료·rate-limit)
    - OpenAI 검증: **GPT-5.4-mini** (~$6.7)
    - Anthropic 검증: **Claude Haiku 4.5** (~$7.8)
  - **전략**: 앵커(Flash-Lite+Gemma)로 개발·반복은 공짜에 가깝게 → 유료 2종은 **최종 프롬프트에서 각 1회만**. 예상 총합 ~$26.5 (여유). 빡빡하면 유료 1종으로 축소($15 안쪽)
  - **제외**: Sonnet 4.6/Opus 4.8/Gemini Pro/3.5 Flash — 1회 $23~$39로 예산 초과. (1주 내 실행 예정; 가격 인하 시 상위 모델 재검토)
  - [x] ~~결정 (2026-07-05): Gemma는 백테스트 제외, forward 전용~~ → **번복 (2026-07-10)**: 시간 여유(보고서 마감까지 2개월+) 감안, 무료 tier 지연을 감수하고 Gemma도 백테스트에 편입. 4차 실험에서 5조건 3,600콜 전량 실행 완료(429 15회, 자동 재시도로 전부 커버) — 상세는 [experiments_log.md](experiments_log.md) 4차 섹션.
  - [x] **provider 분기 + 저장 분리(C1) 구현 완료** — `call_llm(prompt, model)` 접두어 분기(lazy client), `--model` 스레딩, `experiment/{cond}/{model}/`·`analysis/{model}/` 하위폴더, 앵커 결과 이전. Gemini 검증됨
  - [x] `.env`에 `OPENAI_API_KEY`·`ANTHROPIC_API_KEY` 추가 (2026-07-05) → forward에서 4모델 스모크 성공 확인
  - [x] **flash-lite + Gemma 4 백테스트 완료** (4차, 2026-07-10) — 5조건×720콜씩 2모델(flash-lite 재현성 검증 겸용, Gemma 신규) `compare/breakdown/significance --model X` 분석까지 완료. 상세는 [experiments_log.md](experiments_log.md) 4차 섹션
  - [x] **GPT-5.4-mini · Claude Haiku 4.5 백테스트 완료** (6차, 2026-08-06~07) — 2모델 합산 7,200콜 실행 + `compare/significance/breakdown --model X` 분석까지 완료. **멀티모델 4종 세트 완성.** 상세는 [experiments_log.md](experiments_log.md) 6차 섹션
    - 결측: GPT 0건(1차 실행에서 429로 스킵된 28건은 재실행 복구 — 429는 30초 재시도 3회로 복구되지 않으므로 **재실행이 유일한 복구 수단**), Claude 197건(cond1 응답 거절, temp=0라 재시도 무의미 → 결측 확정)
    - 핵심 결과: cond4>컨센서스 **4모델 중 3모델 유의**(GPT만 ns, 단 평균차 +0.69%p로 방향 일치 — Buy 115건 표본부족에 의한 검정력 저하), 리포트 marginal 무의미 **4모델 전부 재확인**
    - 모델 ID 확정: `gpt-5.4-mini`(temperature=0 수용 스모크 확인), `claude-haiku-4-5`
    - ⚠️ Claude API 사용 한도 이력: 6차 실행 중 소진 → 상향 후 복구. 추가 Claude 호출 시 잔여 한도 확인 필요
  - **백테스트 추가 실험 없음 (2026-08-07 확정)**: 멀티모델 4종으로 종결. 신규 모델·신규 조건 백테스트는 계획에 없음
  - ~~**미채택**: cond4_blind (종목명 익명화)~~ — 시가총액·현재가로 부분 식별이 가능해 완전 blind가 성립하지 않으므로 실행하지 않음. 사전학습 편향 검증은 **C-5(cond1 오염 검증)**로 대체
- [ ] Forward Test 검증: 오늘 기준 신호 생성 → **한 달 후 실제 결과 추적** (실전 유효성). 백테스트와 **동일 4모델**로 일관성 유지 (콜 수 적어 비용 무관)
  - [x] 성숙 신호 평가 스크립트 `forward_eval.py` — 신호일 이후 실제 5/20d 수익률·방향/초과 적중 집계 (calc_return 재사용, 미성숙은 pending)
  - [x] 일괄 실행 래퍼 `forward_run_all.py` — 20종목 × 5조건, 앵커(flash-lite), 당일캐시 멱등
  - [x] 입력 검증 `forward_verify.py` — 현재가=FDR최신·ROE정합성·52주·리포트30일·DART신선도 (종목당 1회). 리포트 0건 시 crawl 경고
  - [x] **모델 계획 완료**: 2026-07-05 4모델(flash-lite/gemma-4-31b-it/gpt-5.4-mini/claude-haiku-4-5) forward 실행 완료 — 상세는 [forward_log.md](forward_log.md)
  - [x] `forward_test.py`에 **model 필드 + 파일명(`{ticker}_{cond}_{model}.json`)** 추가 — 멀티모델 forward 파일 충돌 방지 (provider 분기 커밋에서 완료)
  - [x] **forward DART 인메모리 fetch 전환** — `_build_dart_row` 추출 후 `get_today_context`가 CSV 미기록·현재 FY 직접 조회. `data/dart_fundamentals/`는 2023-2025 순수 36행 유지(스모크로 CSV 불변 확인), write 경로(_update_dart_one) 동작 불변. reports는 라이브 유지.
  - **주간 리듬(일요일 저녁)**: ① `crawl.py`(리포트 최신화) ② `forward_run_all.py`(생성) ③ `forward_verify.py`(입력 점검) ④(4주 뒤~) `forward_eval.py`(성숙 평가). ⚠️ **리포트 자동갱신 안 됨 → ① 필수**. 20d 보유 겹쳐 표본 비독립 → 실전 참고용(유의성은 백테스트)
- [ ] app.py 디자인 개선 (최후 — 발표/데모용)
  - [x] 전체 종목 한눈에 보기 탭 추가 (탭2 — forward 캐시 신호 매트릭스, API 호출 없음)
  - [x] **탭1 신뢰성 3건** (2026-07-29): ① 분석 실패 시 직전 종목 결과가 남던 stale 버그 제거 ② 생성 출처 배지(정식 배치 캐시 / 시연 캐시 / 신규 호출) ③ `forward_verify.verify_ticker` 재사용한 입력 검증 배지 — `get_price` 호출이 있어 렌더링마다 돌지 않도록 분석 시 1회만 수행 후 session_state 저장
  - [x] **DART 초기화 lazy 전환** (2026-07-29): `_check_dart_cache()`를 앱 시작 → 분석 버튼 시점으로 이동. 날짜가 바뀐 첫 실행에서 법인코드 약 11MB 재다운로드가 DART와 무관한 탭2·탭3(읽기 전용)까지 묶던 문제 해소. 캐시 반환 경로에서는 호출조차 하지 않음
  - [x] **모델 커버리지 안내** (2026-07-29): 백테스트 완료 모델을 폴더에서 동적으로 읽어 탭1 안내문·탭3 상단 캡션에 명시 — forward 4모델 vs 백테스트 2모델 차이가 누락으로 오인되지 않도록. gpt/claude 백테스트 완료 시 자동 반영됨
  - [x] **시연 신호 격리** (2026-07-29): 앱에서 생성된 forward 신호를 `results/forward_demo/`로 이동해 `forward_eval.py` 평가 표본에서 자동 제외. 계기가 된 07-11 오염 1건과 설계 상세는 [forward_log.md](forward_log.md) 참고
  - [x] `use_container_width` → `width="stretch"` 9곳 (streamlit 1.58 deprecation, 2025-12-31 폐기 예정)
  - [ ] Streamlit Community Cloud 배포 (API 키를 st.secrets로 전환) — 배포 시 탭1은 API 키가 필요하므로 캐시 읽기 전용 데모 모드 분리 검토, 투자 조언 아님 고지 추가

  **잔여 작업 (2026-08-07 확정, 우선순위 순)**

  > 라벨 주의: 아래 앱-1~4는 이 문서 상단의 A-1·B-1(논문 한계·강점 섹션)과 **무관**하다.
  > 대화에서 쓰던 약칭을 괄호에 병기한다.

  - [ ] **앱-1. 탭3 "베이스라인 대비 전체 성과" 표 재구성** *(대화 라벨 B-1)* ← 최우선(시연 사고 방지)
    - 현상: cond1~cond4_no_reports 다섯 줄이 소수점까지 동일(`3.119 / 54.9 / 0.815`). 버그가 아니라 `full_comparison.csv`의 cond 행이 **Buy/Neutral/Sell을 모두 합친 전체 720건**이라 어느 조건이든 같은 종목·같은 날짜 집합이 되기 때문(구조적). 반면 베이스라인 행은 Buy만 집계 → **애플-오렌지 비교**이기도 함
    - 조치: 다섯 줄을 **"시장참여 벤치마크(무기술, 전 종목 월별 보유)" 한 줄**로 접고, LLM은 **Buy 신호 기준**으로 베이스라인과 비교하도록 표를 다시 구성. B-2(무기술 벤치마크 호명) 서술과 직접 연결됨
    - 함께 처리: **4모델 대응**(`model_column_rows` per_row=2 확인), **소표본 방어** — Claude cond2 n=6(Hit 100%·Sharpe 5.60), cond4_no_reports n=1(**Sharpe NaN**), GPT cond2 n=21. 현행 `SMALL_SAMPLE_N=30` 경고로는 NaN 표시가 방어되지 않음
  - [ ] **앱-2. 포트폴리오 백테스트 탭** *(대화 라벨 A-1)* — 신호 평균 → 실제 운용 성과로 격상
    - 매월 Buy 신호 종목 동일가중 매수 → 20거래일 보유. **누적수익 곡선 + MDD + 연환산 변동성 + 지수 대비 곡선**, **거래비용 0.25% 토글**
    - 근거: 현행 지표(수익률·Hit·Sharpe)에 **낙폭(리스크) 축이 없음**. 거래비용 토글은 C-2의 "미구현 결정"을 데이터로 뒷받침
    - 입력: `results/experiment/{cond}/{model}/latest/*.csv` + `data/price/` — **신규 API 호출 없음**
    - 착수 전 결정 필요: 리밸런싱 주기 / 조건별 곡선 분리 방식 / Sell 신호 처리
  - [ ] **앱-3. 조건 간 신호 전이(flip) 분석** *(대화 라벨 A-2)* — 위 **A-2. 방법론 한계**의 "paired test는 future work"를 해소
    - cond3 ↔ cond4, cond4 ↔ cond4_no_reports의 3×3 전이표(Buy/Neutral/Sell) + 뒤집힌 건들의 실제 수익률 비교 + Wilcoxon signed-rank
    - 동일 종목·날짜를 공유하는 표본에 Mann-Whitney(독립 가정)를 쓴 현행 한계를 직접 보완. 데이터는 이미 있음
  - [ ] **앱-4. app.py 파일 분할** — 앱-2(포트폴리오 탭) 착수 직전이 적기. 현재 909줄이고 탭이 하나 더 붙으면 1,200줄 초과
  - [x] ~~**미채택**: 모델 간 신호 합의도 열~~ (2026-08-07) — forward 캐시(4주×4모델, 이미 생성분)를 읽는 것이라 **API 비용은 0**이나, forward를 앱에서 강조하지 않기로 한 결정(아래 "미채택 — forward 성과 탭")과 일관되게 미채택
  - [x] ~~**미채택**: 근거 텍스트 컨텍스트 활용도 분석(연구 챕터용)~~ (2026-08-07) — 화면에 붙일 산출물이 빈도표 하나뿐이고, "언급하지 않음 ≠ 참조하지 않음"이라 결론을 세우기 어려움. 앱-3(flip 분석)이 같은 질문에 더 직접적으로 답함. **단, 파이프라인 검증용으로는 채택** → 6차에서 조건별 키워드 언급률로 컨텍스트 배선 정확성을 실증했고 보고서 데이터 검증 절에 표로 수록 예정([experiments_log.md](experiments_log.md) 6차 "컨텍스트 반영 검증")
  - **미채택 — forward 성과 탭** (2026-07-29 결정): `evaluation.csv`를 앱에 붙이는 대신 **보고서 표로 서술**한다. 이유는 ① 주간 반복으로 20거래일 보유구간이 겹쳐 표본이 비독립이라 유의성 주장이 불가한데 탭 하나를 주면 주력 근거로 오인될 소지가 있고 ② 백테스트(36개월 × 20종목 × 5조건)가 주력, forward는 보조라는 위계를 화면이 흐리기 때문. 정적 CSV라 필요해지면 나중에도 즉시 추가 가능

### C-4. 선택 (재현성·기술부채)
- [ ] 결과 메타데이터(JSON) 저장 루틴
- [ ] 코드 중복 단일화: `COND_LABELS`(compare.py/app.py), `TICKERS`/`SECTORS`(utils→compare 파생)
- [ ] 베이스라인 return_5d 추가 여부 결정 (현재 20d만)

### C-5. cond1 사전학습 오염 검증 (2026-08-07 신설, 6차 발견 3에서 파생)

> **기존 결과 재분석이며 백테스트 재실행이 아님. 신규 API 호출 불필요.**
> 미채택된 cond4_blind를 대체하는 사전학습 편향 검증 경로.

**문제**: cond1(종목명 + 현재가만) Buy 평균이 +8.64%로 전 조건 1위인데, 이를 "무컨텍스트가
가장 우수"로 읽으면 ablation 전체 논리가 무너진다. 그런데 정황은 성능이 아니라 사전학습
기억을 가리킨다.

**정황 (6차에서 확보)**
- cond1에서 방향성 판단을 낸 모델은 **flash-lite 단독**. Gemma·GPT는 720건 전량 Neutral,
  Claude는 523건 전량 Neutral + 197건 응답 거부
- flash-lite cond1 Buy 22건 중 **18건이 실험기간 최대 급등주 2종목에 집중** —
  한화에어로스페이스 14건(+11.13%, hit 78.6%), 알테오젠 4건(+10.40%, hit 75.0%)
  (`results/analysis/gemini-2.5-flash-lite/latest/all_stock_buy.csv`)
- Claude cond1 거절이 **사전학습 친숙도에 따라 갈림**: 삼성전자 0건 거절(36개월 전부 응답)
  ↔ HD현대중공업 27건·두산에너빌리티 26건 거절

**할 일**
- [ ] flash-lite cond1 Buy 22건의 **신호일과 해당 종목 실제 급등 시점 대조** — Buy가 급등
  이전에 나왔는지, 급등이 이미 진행된 뒤에 나왔는지. 급등 구간에 몰려 있으면 기억 가설이 강화됨
- [ ] cond1 Buy 종목 편중을 정량화 (22건 중 2종목 18건 = 81.8%). 우연 여부 확인
- [ ] Claude 거절률과 종목 인지도(시가총액·리포트 수 등 대리변수)의 관계 확인
- [ ] 서술: cond1 성과를 성능 지표에서 제외하고 **오염 통제 지표**로 재배치. B-3(백테스트 vs
  forward 대조)과 한 챕터로 묶음
