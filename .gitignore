# API 키 및 환경변수
.env

# Python 캐시
__pycache__/
*.pyc
*.pyo

# 수집 데이터 (불필요 폴더만 제외)
data/announcements/
docs_cache/

# 앱 시연으로 생성된 forward 신호 (연구 표본 아님 — forward_eval 평가 대상에서 제외됨)
results/forward_demo/

# 앱에서 20종목 밖을 분석할 때 생기는 캐시 (리포트는 최근 30일치, 주가는 조회 이력).
# 백테스트 입력인 20종목 CSV는 이미 추적 중이라 이 규칙의 영향을 받지 않는다.
data/reports/*.csv
data/price/*.csv

# 데이터 파일
*.png

# Claude Code 내부 설정
.claude/

# uv 가상환경 (재현은 uv sync로)
.venv/

# IDE
.vscode/
.idea/

# 개인 문서 일체 (push 안 함, 드라이브 백업 대상)
# 심사 방어 준비 메모, 외부 LLM 리뷰용 브리프, 참고문헌 확정 기록,
# 중간·최종보고서 초안과 양식, 보고서 빌드 스크립트, 그림 원본이 여기 모여 있다.
docs/private/
