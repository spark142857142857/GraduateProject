# API 키 및 환경변수
.env

# Python 캐시
__pycache__/
*.pyc
*.pyo

# 수집 데이터 (불필요 폴더만 제외)
data/announcements/
docs_cache/

# 앱 시연으로 생성된 forward 신호 (연구 표본 아님 — forward_eval 평가 대상에서 제외됨)
results/forward_demo/

# 앱에서 20종목 밖을 분석할 때 받아지는 리포트 캐시 (최근 30일치만).
# 백테스트 입력인 20종목 CSV는 이미 추적 중이라 이 규칙의 영향을 받지 않는다.
data/reports/*.csv

# 데이터 파일
*.png

# Claude Code 내부 설정
.claude/

# uv 가상환경 (재현은 uv sync로)
.venv/

# IDE
.vscode/
.idea/

# 심사 방어 준비용 개인 문서 (push 안 함)
docs/defense_qa.md

# 외부 LLM 리뷰용 컨텍스트 브리프 (개인 작업용, push 안 함)
docs/app_context_for_gpt.md

# 참고문헌 확정 기록 (심사 대비 메모 포함, push 안 함)
docs/references.md

# 중간보고서 초안·양식 (개인 제출용, push 안 함)
docs/*중간보고서*
